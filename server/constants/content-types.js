const QUOTE = 'quote'
const IMAGE = 'image'
const VIDEO = 'video'

const ALL_CATEGORIES = [QUOTE, IMAGE, VIDEO]
module.exports = {
    QUOTE,
    IMAGE,
    VIDEO,
    ALL_CONTENT_TYPES: ALL_CATEGORIES,
}
